---
import ContainerSticky from "@components/container-sticky-bottom.astro";
import Link from "@components/ui/link.astro";
import Dropdown from "./dropdown.astro";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";
import aidxnLogo from 'assets/snacks-logo-light.svg'; 


const menuitems = [

  {
    title: "Let's put your vision into focus.",
    path: "/contact",
  },
  
];
---

<style>
  #navLogo {height: 95px; margin: 5px 0px; filter: drop-shadow(2px 4px 6px #ffffff30);} 
  @media only screen and (max-width: 1199px) {
    #navLogo {
      height: 65px;
    }
  }
  @media only screen and (max-width: 768px) {
    #navLogo {
      height: 45px;
    }
  }
  header { background: rgba(42, 42, 42, 0.1); backdrop-filter: blur(45px); border: 2px solid rgba(42, 42, 42, 0.2); box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05); }
  .dashboard-button {display: none;}

  h1 { color: white; mix-blend-mode: difference; }
</style>

<ContainerSticky>
  <header class="flex flex-col lg:flex-row justify-center items-center px-2 md:px-6 lg:px-8">
    <Astronav>
      
      <MenuItems class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0">
        <ul class="flex flex-col lg:flex-row lg:gap-4">
          {
            menuitems.map((item, index) => (
              <>
                {item.children && (
                  <Dropdown
                    title={item.title}
                    children={item.children}
                    lastItem={index === menuitems.length - 1}
                  />
                )}

                {!item.children && (
                  <li>
                    <a
                      href={item.path}
                      class="flex lg:px-3 py-1 md:py-4 text-lg md:text-xl text-gray-300 hover:text-gray-100"
                    >
                      <h2>
                        {item.title}
                      </h2>
                    </a>
                  </li>
                )}
              </>
            ))
          }
        </ul>

        
      </MenuItems>
    </Astronav>
    
  </header>
</ContainerSticky>

